<h2>Флот:</h2>

<div class="block">
  <%= form_for(@vessel, :url => admin_vessels_path(@vessel)) do |form| %>
    <fieldset class="clearfix form-horizontal">
      <legend>Новое судно</legend>
      <%= render 'form', :f => form %>
    </fieldset>
  <% end %>
</div>