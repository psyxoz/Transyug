<h2>Вакансии:</h2>

<div class="block">
  <%= form_for(@job, :url => admin_jobs_path(@job)) do |form| %>
    <fieldset class="clearfix form-horizontal">
      <legend>Новая вакансия</legend>
      <%= render 'form', :f => form %>
    </fieldset>
  <% end %>
</div>